name: ExcelCoordinate workflow

on:
  push:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: build Module
        # env: 
        #   PSGALLERY_API_KEY: ${{ secrets.PSGALLERY_API_KEY }}
        shell: pwsh
        # run: ./cp-21yamExcelCoordinate.build.ps1
        run: Invoke-Build -Task ModuleBuild
